[project]
name = "query-routing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "michaeljwalla", email = "michaeljwalla@gmail.com" }]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
query-routing = "query_routing:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = true
