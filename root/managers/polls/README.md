`ratings.csv` is not live-updated (must be regenerated)
`queue.txt` is loaded into memory & updated live
`misc/` contains polls of unchecked format (JSON)
`ratings/` contains rating polls & those remapped as such through the site.